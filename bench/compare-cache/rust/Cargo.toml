[package]
name = "cache-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
moka = { version = "0.12", features = ["sync"] }
quick_cache = "0.6"
rand = { version = "0.8", features = ["small_rng"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
